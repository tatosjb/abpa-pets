<p></p>

<div class="container col-xs-12">
  <p class="pull-right">
    <button class="btn btn-primary" data-toggle="modal"
            data-target="#newAnimalModal">Novo animal</button>
  </p>
</div>

<div class="col-xs-12"><%= will_paginate @animals %></div>

<% @animals.each do |animal| %>
    <%= render :template => "animals/animal", :locals => {:animal => animal} %>
<% end %>

<div class="col-xs-12"><%= will_paginate @animals %></div>

<%= render :template => "animals/new", :locals => {:@animal => Animal.new} %>

