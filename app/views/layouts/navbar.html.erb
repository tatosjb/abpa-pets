<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">ABPA</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if user_signed_in? %>
          <ul class="nav navbar-nav">
            <li><a href="/">Animais</a></li>
            <li><a href="<%= people_path %>">Pessoas</a></li>
          </ul>
      <% end %>
      <div class="navbar-right">
        <form action="<%= animals_path %>" class="navbar-form">
          <div class="form-group">
            <input name="q" type="text" placeholder="Pesquisar animal" class="form-control">
          </div>
          <button type="submit" class="btn btn-success">Pesquisar</button>
        </form>
      </div>
      <div class="navbar-right">
        <ul class="nav navbar-nav">
          <li><%= link_to 'Perfil', edit_user_registration_path, :class => 'navbar-link' if user_signed_in? %></li>
          <li><%= link_to "Sair", destroy_user_session_path, method: :delete, :class => 'navbar-link' if user_signed_in? %></li>
        </ul>
      </div>
    </div><!--/.navbar-collapse -->
  </div>
</nav>
